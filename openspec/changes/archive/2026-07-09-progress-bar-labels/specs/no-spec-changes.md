## ADDED Requirements

No new capabilities. This change is a pure UI enhancement — it consumes existing data (`position_secs`, `duration_secs` from the `player-status` event) and adds no new backend behavior.

## MODIFIED Requirements

None. Existing specs (`playback-position`, `track-transition`) are unaffected.

## REMOVED Requirements

None.
